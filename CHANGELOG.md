/* Pull the client's own tokens into the toolkit site so the guidelines are
   themselves an example of the brand. Update these to match brand.json —
   or generate this file from it with Style Dictionary. */
:root {
  --sl-color-accent: #000000;        /* colour.primary */
  --sl-color-accent-low: #000000;
  --sl-color-accent-high: #000000;
  --sl-font: system-ui, sans-serif;  /* font.body */
  --sl-font-system: var(--sl-font);
}
