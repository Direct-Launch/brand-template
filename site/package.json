{
  "name": "brand-toolkit-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "tokens": "node ../scripts/build-tokens.mjs",
    "sync": "node ../scripts/sync-content.mjs",
    "prebuild": "npm run tokens && npm run sync",
    "predev": "npm run tokens && npm run sync",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.30.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.0"
  }
}
